<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:EarthBackground.ViewModels"
             xmlns:loc="using:EarthBackground.Localization"
             x:Class="EarthBackground.App"
             x:DataType="vm:MainWindowViewModel"
             RequestedThemeVariant="Light">

    <Application.Styles>
        <FluentTheme />
    </Application.Styles>

    <TrayIcon.Icons>
        <TrayIcons>
            <TrayIcon Icon="/Assets/earth.ico"
                      ToolTipText="EarthBackground">
                <TrayIcon.Menu>
                    <NativeMenu>
                        <NativeMenuItem Header="{loc:Localize TrayMenu_ShowWindow}"
                                        Command="{Binding ShowMainWindowCommand}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem Header="{loc:Localize TrayMenu_Exit}"
                                        Command="{Binding ExitCommand}" />
                    </NativeMenu>
                </TrayIcon.Menu>
            </TrayIcon>
        </TrayIcons>
    </TrayIcon.Icons>

</Application>
